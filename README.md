gh-pages for scifabric.com

How to preview locally:

* Install latest ruby, e.g. with rvm
* Install bundler
* bundle install

```
gem install bundler
bundle install
```

Preview page:

```
jekyll serve
```

